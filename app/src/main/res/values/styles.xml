<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Widget.CustomSearchView" parent="Widget.AppCompat.SearchView">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/darker_gray</item>
        <item name="queryBackground">@android:color/transparent</item> <!-- opcional, quita fondo gris -->
    </style>

</resources>